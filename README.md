Project Setup (React + TypeScript + Ant Design + Router + Aliases)

This project is a React + TypeScript app bootstrapped with Vite, styled with Ant Design, and structured with Feature-Sliced Design (FSD) using custom path aliases (@shared, @widgets, etc.).

📦 Tech Stack

FSD - file structure

React 18 – UI library

TypeScript – static typing

Vite – fast build tool

Ant Design (antd) – UI components

Tailwind - styling

React Router v6 – routing

Path Aliases – clean imports (@shared, @widgets, @entities, etc.)